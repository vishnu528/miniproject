export default function UserCards(props){
    return(
        <> 
        <h2>this line is from component UserCards and probs values are name:{props.name} and age: {props.age}</h2>
        </>
    )
}